@require: list

module Mathml :> sig

  val \math : inline [math-text]
  val output-scripts : string -> (option string) -> (option string) -> string

end = struct

  val inline tinfo \math m = stringify-math tinfo m

  val output-scripts base sub-opt sup-opt = base

end
